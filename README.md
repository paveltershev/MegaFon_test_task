Тестовое задание MegaFon: Прогноз погоды
Приложение парсит прогноз погоды на 7 дней с Яндекс.Погода, сохраняет данные в Excel и 
логирует запросы в SQLite.

 Важно: Яндекс.Погода активно защищает публичный сайт от автоматизированных запросов 
 (SmartCaptcha, JavaScript-проверки).
В демонстрационных целях парсер работает на заранее сохранённой HTML-странице 
 (samples/yandex_moscow.html), полученной после ручного прохождения CAPTCHA.
В production-среде рекомендуется использовать официальное B2B API Яндекса.
 
Функционал
Парсинг данных на 7 дней (4 периода в день: утро, день, вечер, ночь)
Экспорт в Excel: weather_report_{город}.xlsx
Логирование всех запросов в SQLite: weather_requests.db
Простая, самодостаточная архитектура без внешних зависимостей
📦 Установка
Убедитесь, что у вас установлен Python 3.8+.

Установите зависимости:
pip install -r requirements.txt


Запуск
python src/main.py "Москва"

Поддерживается любой город — но данные всегда берутся из офлайн-файла 
(samples/yandex_moscow.html), так как онлайн-запросы заблокированы CAPTCHA.